/**
 * 定时炸弹 - 空盒子
 */

import BulletParent from "../BulletParent";

const {ccclass, property} = cc._decorator;

@ccclass
export default class TimeBombBox1Bullet extends BulletParent {

    
    //重写update
    update (dt) 
    {
    }

    
}
